First stage: compute German taxes

TODO: First tax approvimation

Review the order of todos
-----------------
TODO: Real problem two assets with the same name and both are real USDS is Sky.money but also TheStandard USD (this one I used) so this is real collision not spam tokens. For now using bigger one as both are stable coins. But will have to implement real token service.
TODO: Need a better valuation rules. If it's something like USDC - BSX trade. Despite BSX being on cmc probably price there is far from real and shouldn't be used. Introduce "strong assets" that even if there is opisite price aren't moved.
TODO: Might need something better than globally unppricable coin. I belive CMC might have good price but only for some time. So I could depend on it but not every time, maybe introduce marker that woudl let me manually mark resolve price using stronger leg (or process on FE where I could click and system would compute this and let me save correction)
TODO: Code that adjust prices by tier is not clear to me. Vibed it quickly.
TODO: Maybe need to redo opening balances and have a separate entity for that. I think there will be a problem with prices for price service.
TODO: Price asset-id normalization (upper-casing) happens three times on the same call path:
  PriceService.rate uppercases base/quote, PriceResolver.fetch_record uppercases them again, and
  each client's fetch_record uppercases once more (e.g. CoinMarketCapClient, OpenExchangeRatesClient).
  Every layer defends itself instead of trusting the caller. Pick one owner (the PriceService entry
  point is the natural one) and let the resolver + clients assume already-normalized AssetIds.
TODO: Handle Polygon situation. Was moving founds there through kraken(visible) and Coinbase and Coinbase PRO. As coinbase pro is not handled I'm missing data. Use manual flow find those tx in koinly. Add as manual transaction deposi EUR, buy USDC (use real price) and transform to second wallet
TODO: An override on EUR (or another valuation anchor) is accepted and wins over the base==quote rate, silently skewing anchor totals; reject anchor overrides and hide the Set price button on those legs.
TODO: Reconsider the separate price_overrides.db (third SQLite file): merging it into corrections.db would drop the parallel engine/sessionmaker/lifespan/fixture stack and allow joins with corrections, but it requires starting to write migrations, so there is a cost.
TODO: EventOrigin<->(origin_location, origin_external_id) column conversion is hand-rolled in four db modules (ledger_events, ledger_corrections, acquisition_disposal, price_overrides); extract a shared mixin/helper in db/.
TODO: UI events flow cleanups: the three price-override mutation props are drilled through VirtualizedDateSections and TimestampBucketRow (which never use them) while reads already go through PriceOverridesContext -- move handlers + pending flag into a provider mounted in Events.tsx (like CorrectionHighlightProvider); Events.tsx also holds five near-identical setFeedback/setPending/try/catch/router.refresh mutation handlers and two variants of the re-run hint message -- extract one shared mutation helper.
TODO: Handle orphaned price overrides. An override whose event_origin matches no corrected event a separate page listing all stored overrides with the ability to delete them, highlighting the orphaned ones (overrides whose event_origin is absent from /corrected-events -- a set difference the UI can compute from data it already fetches).
TODO: Case of sending NEU to no longer existing exchanges. Similar case would be when I send some value to Polygon and ten retrved it. One idea is to have own "fake wallet" that will store those and some operations happened there.
TODO: We introduced pegged stables, but it might be mistake. Stables sometimes depeg so maybe we should treat them as other coins and limit valuation only to stables.
TODO: ui/src/components/SystemState and ui/src/components/WalletProjection are vibed not checked and are very bad but should be refactored.
TODO: In valuation during acquisition / disposal matching I have a mid-point rule which might be not correct for Germany. Review it. Even if it is correct review the logic and tests again initial task was done a bit fast.
TODO: Maybe it would make sense to add eur value to corrected events lane. Code that does price valuation exists in disposal. Maybe it should be stored in db to do it only once? Maybe this part of code should be executer earlier, not during acq / disposal matching
TODO: Possible name cleaning. There are accounts with its endpoint and there is a wallet projection with another endpoint. Those carry different semantics.
TODO: Potential problem Coinbase / Moralis on Coinbase it's CBETH, on Moralis it's cbETH. Proper solution would be this token registry, but for now maybe we can live with lowercasing all token names.
TODO: Need automation to rerun computation (src/main.py) through the UI see if https://fastapi.tiangolo.com/tutorial/background-tasks/ could be useful. This could get a bit more complicated. I would like to have a reporting of problems with clear information and error types. Then I could handle such problems in the UI. For example, report there is a missing amount, then I could click and open a window with token history and tx that failed. For missing price I could open different window with price manipulations. etc
TODO: Find a way to remove spam transactions by looking at them from a token perspective. The first idea is to create a page with all events for a token then those could be removed.
TODO: Identify places where I went out and came back from other blockchains. Polygon, Fantom/Sonic, Uni. The first idea is to introduce "fake" wallet for such cases. Then I can model blockchain operations correctly, now they will become acquisitions / disposals.
TODO: Think about a cache for old transactions in case Moralis would die and I wouldn't have data. Or Coinbase would start limiting history
TODO: In a few places there are functions like _decimal_from_atomic_value unify them into one place. There is decimal_from_atomic_value in data/src/utils/misc.py
TODO: Idea to review. Add the ability to quickly navigate to the first failing transaction.
TODO: Add value of current wallet balances
TODO: Implement more sophisticated logic for tokens to handle this logic when a token with the same name on different chains might have different value and those are not spam tokens. I think its fGHO case (fluid repos)
TODO: Token registry topic. CMC has problem resolving tokens by token name, we also have coinbase and kraken at some point binance.
TODO: Another review of fee legs. Now without is_fee (or equivalent semantics), residual same-asset differences in transfer-like events (onchain with gas) require partial leg decomposition, and the current engine can only classify whole legs, causing misclassification of internal transfers as taxable disposals/acquisitions.
TODO: Hide checkboxes for raw events that already have a correction.
TODO: In tests there are two or three places where test instances LedgerEvents are generated. Those should be combined into one helper.
TODO: Tests are using the same rng seed (0). Seed should be randomized during each test run and printed for duplication. Probably there is some pytest plugin for this.
TODO: Maybe split TS types - different one for LaneItem different one for rendered items. Inheritance vs. composition. Still, due to structure, inheritance seems ok. Now I think I have partial composition. Still, maybe this should be made more verbose
TODO: Revaluate idea of replace selectedEventOriginKeys / getSelectedEvents with one map like it was before.
TODO: Thing to think about: Should page.tsx transform data to for format that is used by the Events component? It is transformed further there.
TODO: A lot of duplication in handler of API communication in the Event component, also not sure about error handling in ui/src/api/core.ts
TODO: Backups of databases they are starting to contain manual work.
TODO: Should I collapse ETH fee when there is an operation that is sending / receiving ETH?
TODO: Review Kraken importer after event type removal and wallets structure changes
TODO: When turning corrections lane on and off virtualized list gets a bit broken. Something about height computation.
TODO: Review the setup of databases in tests. Especially now we have one setup for api tests and one for other. Looks bad.
TODO: Generally tests suck, think about how to improve it.
TODO: I think some accounts I flagged as mine are actually not mine, but Coinbase
TODO: code that creates legs for native and erc20 is similar to one for native transfers we could improve
TODO: Is there a possibility to have a whitelist of commands for codex?
TODO: Add to my ~/.codex/AGENTS.md instruction to after making changes try to read general context and see if there could be some code refactoring done to reduce tech debt like code duplication or similar.
TODO: add loaders to the UI when data is being fetched, especially when changing visible columns
TODO: we will end with multiple ledger legs tables in the database - maybe combine them into one with a discriminator column?
TODO: Need manual exception on taxes for some transactions. Example when Neufund gave me BTC/ETH as a bonus.
TODO: mark non-taxable events (income), next layer of corrections
TODO: Do I still need it? Maybe a good UI is enough? A tool to print from db event, legs etc. for debugging. After provided id print whole network of entities
TODO: I'm suspicious of the tax calculations. Think how we can debug it better.
TODO: Restore inventory taxable and free.
TODO: Do we need extra snapshot mechanism to, for example, output value of wallets?
TODO: Review inventory again. I don't like those intermediate objects also ones in taxes.
TODO: Verify against my own spreadsheet
TODO: How to calculate income and taxes in case when I'm partly leaving the farm.
 Initial idea (from old model review)
 - ctokens only when token is sold compute gain as difference of value when bought (FIFO used for computing buy price)
 - atokens compute tax / gain every time it rebases and when selling no tax
TODO: General problem how to treat rebase tokens. For now, Manually add missing amount when leaving the farm
TODO: How to calculate Uniswap style (with NFT)
TODO: How to calculate Vfat style with subcontract I think it doesn't store any tokens.
TODO: Kraken scripts maybe move them to a separate directory, some common code can be extracted?
TODO: DB improvements.
 - use Enums
 - use UUID type
TODO: Export data in a format that can be used by Steuerberater
TODO: Date chooser could be improved (in far futuren when I will need to make it nice)
 - it could follow (open/close) when the main section is being scrolled
 - active state of year and month should be properly stated
TODO: Features from WEALTH_MODEL.md
 - income
 - realized P/L
 - unrealized P/L (by asset?)
TODO: Search function in the UI, tx hash
TODO: Tracking of BTC through some tracker. This probably could be skipped as I'm doing enough transactions, and it's too much work.
TODO: Wallet tracking loses Ethereum intra-block ordering. Moralis cache has the correct order for same-block transactions using block_number and transaction_index, but once transactions are converted into LedgerEvents that metadata is dropped. Later rebuild stages sort only by timestamp and then lexicographically by hash/origin, so same-block events can be replayed in the wrong order. In this case the ETH-T -> ETH swap is processed before the prior tx that credits 7 ETH-T, which creates a false insufficient-balance failure in wallet tracking. https://etherscan.io/tx/0x9795e90368cf6e4edacf33e06d455cb94cc4983f10ea0b09e050a899adf82488 and https://etherscan.io/tx/0x41b99a85b5b52e1ef62ab9de87a2a74fb5f779175a0e0eb0165b8dc6904b6156
TODO: In private.txt have ARB tx that does a lot of operations on vfat / pancake and liquidity
TODO: Coinbase rounding problem. Probably nothing to fix. Question it should be fixed with replacing tx or with extra correction tx. The second one more visible?
TODO: Coinbase handling should be improved. For now, it's plumbed with extra corrections and fixing can wait.
  - Buying currency paying with CC. Then there is one negative for EUR and a positive for crypto. This breaks wallet tracking.
  - There are few to / from the pro-account, and this also seems to be a problem. Dates to review 2022-02-16 and 2022-08-04. I think I bought 10.087326USDT with EUR moved a bit, then a few months later converted it to ETH
  - There is also a mysterious 0.18 EUR missing on 2023-01-12.

Codex commands
------------------
npm list -g @openai/codex
npm install -g @openai/codex@latest
https://github.com/openai/codex/releases

npm list -g @openai/codex && npm install -g @openai/codex@latest && npm list -g @openai/codex && codex

Libraries docs
------------------
https://getbootstrap.com/docs/5.3
https://react-bootstrap.netlify.app
https://react.dev/reference/react

Some loose ideas
------------------
- Ability to compute / enter the current value of open positions.
- Store history / snapshots of the state to catch changes in time

- Can system handle such use case, handle in terms of following transactions but also to compute
  the current state and profit (two or more currencies?) from the position
  - farming / borrowing on xlend.extrafi.io
    - supply cbBTC on Base
    - extra incentive through Merkl that gives
      - OP on Optimism
      - xOP on Optimism staked on xlend.extrafi.io
      - some EXTRA tokens on Optimism
    - occasionally claimed and swapped to WBTC (no cbBTC on Optimism)
    - then jumper that moves and trades it to cbBTC on Base
    - supply cbBTC to farm to decrease debt
    - borrowing USDC in xlend.extrafi.io
    - putting it into farm 40acres USDC Vault through vaults.fyi
    - occasionally get some money from the farm and repay part of the debt to decrease debt
- Idea how to track and handle things like stakewise, but also uniswap. Introduce tracking of NFT as a thing I own and has its value. Just one single token that can be traded against its content.
  - Question how to treat partial deposits and witdrawals.
  - For stakewise it could be a "virtual" token but with the same mechanics
- vfat scythe is "my" contract where I transfer tokens that buys those "entities" from point aboge

- Polish bonds income!!!!

Other projects
------------------
- https://github.com/eprbell/rp2
- https://github.com/BittyTax

- https://app.koinly.io/p/transactions
- https://cointracking.info/
- https://app.summ.com


Source of onchain transactions
------------------
https://docs.moralis.com/

https://thegraph.com/
https://docs.etherscan.io/introduction
https://docs.cloud.debank.com/en/

german taxes writep

https://www.bundesfinanzministerium.de/Content/DE/Downloads/BMF_Schreiben/Steuerarten/Einkommensteuer/2025-03-06-einzelfragen-kryptowerte.html
https://www.bundesfinanzministerium.de/Content/DE/Downloads/BMF_Schreiben/Steuerarten/Einkommensteuer/2025-03-06-einzelfragen-kryptowerte-englisch.pdf?__blob=publicationFile&v=3